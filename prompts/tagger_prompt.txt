You are an expert radiology report tagger. Read the following radiology report and output ONLY a JSON object with the specified fields.

### Instructions

1.  **Analyze the Report**: Carefully read the entire radiology report.
2.  **Extract Key Information**: Identify the exam modality, contrast status, body region, study purpose, and overall normality.
3.  **Generate JSON**: Construct a JSON object with the following fields and values.

### JSON Fields

-   `exam_modality`: (Enum) High-level imaging type.
    -   Example values: `XR`, `CT`, `MR`, `US`, `NM`, `PET`, `Mammo`, `Fluoro`, `IR`, `Other`
-   `contrast_status`: (Enum) How contrast is used.
    -   Example values: `no_contrast`, `iv_contrast`, `oral_contrast`, `iv_and_oral`, `intra_articular_or_local`, `not_applicable`, `unknown`
-   `body_region`: (Enum) Primary anatomic region of the study.
    -   Example values: `head_neck`, `brain`, `chest`, `cardiac`, `abdomen`, `pelvis`, `abdomen_pelvis`, `spine_cervical`, `spine_thoracic`, `spine_lumbar`, `upper_extremity`, `lower_extremity`, `whole_body`, `other`, `unknown`
-   `study_purpose`: (Enum, optional) Coarse clinical context.
    -   Example values: `screening`, `diagnostic_new_problem`, `follow_up_known_disease`, `preoperative_or_planning`, `trauma`, `other`, `unknown`
-   `normality`: (Enum) The overall finding of the report.
    -   Example values: `normal`, `abnormal`, `limited_or_nondiagnostic`, `uncertain`
-   `exam_type_string`: (String) A concise, free-text description of the exam (e.g., "CT chest with IV contrast", "Noncontrast CT head").
-   `needs_downstream_disease_analysis`: (Boolean)
    -   `true` if `normality` is `abnormal` or `uncertain`.
    -   `false` if `normality` is `normal`.

### Report to Process: {report_text}
